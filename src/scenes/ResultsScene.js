import Phaser from 'phaser';

class ResultsScene extends Phaser.Scene{
    constructor(config){
        
        super('ResultsScene')
        this.config = config
    }

    create(){

    }
}

export default ResultsScene