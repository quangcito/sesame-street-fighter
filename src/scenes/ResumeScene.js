import Phaser from 'phaser';

class ResumeScene extends Phaser.Scene{
    constructor(config){
        super('ResumeScene')
        this.config = config
    }

    create(){
    }
}

export default ResumeScene