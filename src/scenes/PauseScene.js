import Phaser from 'phaser';

class PauseScene extends Phaser.Scene{
    constructor(config){
        
        super('PauseScene')
        this.config = config
    }

    create(){

    }
}

export default PauseScene